s3:
  raw:
    format: json
    compression: gzip
    partition_cols: ["year", "month", "day"]

  processed:
    format: parquet
    compression: snappy
    partition_cols: ["year", "month", "day"]

  analytics:
    format: parquet
    compression: snappy
    partition_cols: ["year", "month", "day"]
   